# CuckooM's Blog — 页面重设计需求分析报告

> 文档版本: v1.0
> 创建日期: 2026-06-01
> 作者: AI 产品经理

---

## 一、需求背景

### 1.1 当前博客概况

| 项目 | 详情 |
|------|------|
| 框架 | Hexo 8.0+ |
| 主题 | landscape（Hexo 默认主题，基于此做定制） |
| CSS | Stylus |
| 语言 | 中英文双语 (zh-CN / en) |
| 部署 | rsync → 47.98.148.158 (www.cuckoom.com) |
| 当前文章数 | 约 6 篇（含中英文版本） |

### 1.2 当前页面结构分析

```
+--------------------------------------------------+
|  HEADER (banner.jpg 背景图 + 居中标题/副标题)      |
|  [导航: 首页 归档 分类 标签 关于] [语言切换] [GitHub] [搜索] |
+--------------------------------------------------+
|                    |                             |
|   右侧边栏          |      主内容区               |
|   - 分类            |      - 首页: 文章列表       |
|   - 标签            |      - 文章页: 正文+评论    |
|   - 标签云          |      - 归档页: 时间线列表   |
|   - 归档            |      - 分类/标签页: 文章列表|
|   - 最新文章        |      - 关于页: 纯文本       |
|   - 搜索            |      - 搜索页: 搜索框+结果  |
|                    |                             |
+--------------------------------------------------+
|  FOOTER (深色背景, 版权信息)                        |
+--------------------------------------------------+
```

### 1.3 现有设计痛点

| 痛点 | 描述 |
|------|------|
| 视觉过时 | landscape 是 Hexo 默认主题，设计语言较陈旧 |
| Banner 图 | 固定 banner.jpg 缺乏灵活性，未体现个人品牌 |
| 边栏臃肿 | 5 个 widget 常驻右侧，压缩主内容阅读空间 |
| 无深色模式 | 不支持夜间阅读场景 |
| 卡片缺失 | 首页文章纯文本列表，缺乏信息层次和视觉吸引力 |
| 移动端体验 | 移动端导航为侧滑菜单，交互可优化 |

### 1.4 已有定制资产（需保留/适配）

- 中英文双语菜单系统 (`theme_config.menu`)
- 语言切换器（带 flag icon 的 dropdown）
- 本地搜索功能（search.xml + 内嵌 JS）
- 自定义页面样式 (`custom.styl`, `custom/pages.styl`)
- 标签/分类计数徽章样式
- 空状态页面样式 (en 无文章时的 empty state)
- Favicon 资源集

---

## 二、功能目标

### 2.1 核心目标

| 目标 | 描述 | 成功指标 |
|------|------|----------|
| 视觉现代化 | 采用现代极简设计语言 | 视觉风格与设计参考一致 |
| 布局重构 | 从"固定边栏+内容"改为"居中卡片+可折叠边栏" | 首屏内容密度提升 |
| 深色模式 | 支持系统级/手动切换 | 深色模式覆盖所有页面 |
| 阅读体验优化 | 提升文章可读性 | 行高、字号、对比度达标 |
| 双语兼容 | 中英文切换后布局一致 | 两种语言下页面结构无差异 |

### 2.2 非目标（本期不做）

- 评论系统开发/更换
- 新的页面类型（如项目展示页）
- SEO 元数据优化
- 图片懒加载 / 性能优化
- 新的插件集成

---

## 三、用户故事

### US-01: 首页卡片式文章列表

```
作为 博客访客
我想要 在首页看到卡片式的文章列表（含标题、摘要、日期、标签）
以便 快速浏览并选择感兴趣的文章阅读
```

**验收标准:**
- [ ] 每篇文章以卡片形式展示
- [ ] 卡片包含：标题、发布日期、分类标签、摘要（前 N 行/截断）、"阅读更多"按钮
- [ ] 卡片有 hover 效果（微阴影/微位移）
- [ ] 首页每页展示 10 篇文章（保持现有分页逻辑）
- [ ] 卡片间距统一，页面左右留白对称
- [ ] 移动端卡片为单列，桌面端可考虑双列

### US-02: Hero 区域（纯文字+渐变背景）

```
作为 博客访客
我想要 在首页顶部看到一个简洁的 Hero 区域（包含博客标题、副标题、个人简介）
以便 快速了解博客定位和作者身份
```

**验收标准:**
- [ ] 移除 banner.jpg 背景图
- [ ] 使用渐变色背景（主色系）
- [ ] Hero 区域包含：博客标题、副标题、简短介绍文字
- [ ] Hero 区域高度不超过视口的 50%
- [ ] 文字颜色与背景对比度 ≥ 4.5:1 (WCAG AA)
- [ ] Hero 区域在非首页（文章页、归档页等）也保持一致样式，但可简化显示

### US-03: 可折叠边栏

```
作为 博客访客
我想要 通过点击按钮展开/收起侧边栏（含分类、标签、归档等）
以便 在需要导航时展开，阅读时收起以获得更大阅读空间
```

**验收标准:**
- [ ] 默认状态：边栏收起（不占据主内容区空间）
- [ ] 提供明显的展开/收起按钮（固定位置或随 Header）
- [ ] 展开后：边栏从右侧滑入或覆盖部分内容区
- [ ] 边栏内包含：分类列表、标签云、归档、近期文章
- [ ] 点击页面内容区域或关闭按钮可收起边栏
- [ ] 移动端：边栏全屏覆盖，带关闭按钮
- [ ] 桌面端：边栏宽度不超过 320px

### US-04: 深色模式支持

```
作为 博客访客
我想要 切换深色/浅色主题
以便 在不同光线环境下都能舒适地阅读
```

**验收标准:**
- [ ] 支持系统级深色模式自动跟随 (`prefers-color-scheme`)
- [ ] 提供手动切换按钮（Header 中的月亮/太阳图标）
- [ ] 切换状态持久化（localStorage）
- [ ] 深色模式覆盖所有页面：首页、文章页、归档页、分类/标签页、关于页、搜索页
- [ ] 深色模式下：背景色、文字颜色、卡片阴影、链接颜色、代码块背景均需适配
- [ ] 深色模式下对比度 ≥ 4.5:1 (WCAG AA)
- [ ] 切换过程有平滑过渡动画（≥ 0.3s）

### US-05: 文章详情页优化

```
作为 博客读者
我想要 在文章详情页获得舒适的阅读体验（合适的字号、行高、代码高亮、导航）
以便 专注阅读内容
```

**验收标准:**
- [ ] 正文内容居中，最大宽度不超过 720px（桌面端）
- [ ] 正文字号 ≥ 16px，行高 ≥ 1.6
- [ ] 文章顶部显示：标题、发布日期、分类、标签
- [ ] 文章底部显示：上一篇/下一篇导航
- [ ] 代码块保持语法高亮，深色模式下自动切换主题
- [ ] 文章内容区左右留白对称
- [ ] 图片居中对齐，最大宽度不超过内容区宽度

### US-06: 归档/分类/标签页重构

```
作为 博客访客
我想要 在归档/分类/标签页面以清晰的结构浏览文章
以便 按时间、主题或标签查找内容
```

**验收标准:**
- [ ] 归档页：按年月分组，时间线样式
- [ ] 分类页：分类卡片列表，点击展开/进入分类文章列表
- [ ] 标签页：标签云 + 标签列表两种视图
- [ ] 各页面顶部有页面标题和描述
- [ ] 样式与首页卡片风格一致

### US-07: 导航栏重构

```
作为 博客访客
我想要 一个简洁的顶部导航栏
以便 快速在不同页面间切换
```

**验收标准:**
- [ ] 导航栏固定在页面顶部（sticky 或 fixed）
- [ ] 包含：博客 Logo/标题、导航链接（首页、归档、分类、标签、关于）
- [ ] 右侧包含：语言切换按钮、深色模式切换按钮、GitHub 链接、搜索按钮
- [ ] 滚动时导航栏有背景色/阴影变化（滚动检测）
- [ ] 移动端：汉堡菜单展开导航链接
- [ ] 导航链接在当前页面有高亮状态

### US-08: Footer 重构

```
作为 博客访客
我想要 一个简洁的底部 Footer 区域
以便 查看版权信息和技术栈
```

**验收标准:**
- [ ] Footer 包含：版权信息、"Powered by Hexo" 标识
- [ ] 深色模式下 Footer 背景色适配
- [ ] 布局居中，与整体风格一致

---

## 四、设计系统规范

### 4.1 色彩体系

| 用途 | 浅色模式 | 深色模式 |
|------|---------|---------|
| 主色 (Primary) | `#4A90D9` | `#64B5F6` |
| 背景色 (Background) | `#FAFAFA` | `#1A1A2E` |
| 卡片背景 | `#FFFFFF` | `#16213E` |
| 文字主色 | `#333333` | `#E0E0E0` |
| 文字次色 | `#666666` | `#A0A0A0` |
| 边框/分隔线 | `#E8E8E8` | `#2D2D44` |
| 链接色 | `#258FB8` | `#64B5F6` |
| 渐变起始色 | `#4A90D9` | `#1A1A2E` |
| 渐变结束色 | `#7C5CBF` | `#16213E` |

### 4.2 字体规范

| 用途 | 设置 |
|------|------|
| 正文字体 | `-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans SC", sans-serif` |
| 正文字号 | 16px |
| 正文字重 | 400 |
| 标题字重 | 600-700 |
| 行高 | 1.6em (正文), 1.2em (标题) |
| 代码字体 | `"Source Code Pro", Consolas, Monaco, "Noto Sans Mono", monospace` |

### 4.3 间距体系

基于 8px 网格：

| 变量 | 值 | 用途 |
|------|-----|------|
| xs | 4px | 元素内紧凑间距 |
| sm | 8px | 列表项间距 |
| md | 16px | 段落间距 |
| lg | 24px | 卡片内边距 |
| xl | 32px | 区块间距 |
| 2xl | 48px | 页面大区块 |
| 3xl | 64px | Hero 区域上下间距 |

### 4.4 断点

| 断点 | 宽度 | 用途 |
|------|------|------|
| Mobile | ≤ 767px | 单列布局 |
| Tablet | 768px - 1023px | 单列/自适应 |
| Desktop | ≥ 1024px | 多列布局 |

### 4.5 圆角与阴影

| 元素 | 圆角 | 阴影 |
|------|------|------|
| 卡片 | 12px | `0 2px 8px rgba(0,0,0,0.08)` |
| 按钮 | 8px | 无 (hover 时加阴影) |
| 头像/图标 | 50% (圆形) | 无 |
| 输入框 | 8px | `0 0 0 1px border-color` |
| 卡片 hover | 12px | `0 4px 16px rgba(0,0,0,0.12)` |

---

## 五、业务流程

### 5.1 页面结构流程

```
[用户访问首页]
    |
    v
[Hero 区域: 博客标题 + 副标题 + 简介]
    |
    v
[文章卡片列表: 每页 10 篇, 卡片含标题/摘要/日期/标签]
    |
    +--> [分页导航]
    |
    v
[点击卡片] --> [文章详情页]
    |                 |
    |                 v
    |          [文章正文 + 顶部元信息 + 底部导航(上/下篇)]
    |
    v
[Header 操作]
    |
    +--> [语言切换] --> [切换到对应语言版本]
    +--> [深色模式切换] --> [切换主题]
    +--> [展开边栏] --> [显示分类/标签/归档/最新文章]
    +--> [搜索] --> [弹出搜索面板]
```

### 5.2 页面清单

| 页面 | 路径 (zh) | 路径 (en) | 主要组件 |
|------|-----------|-----------|---------|
| 首页 | `/` | `/en/` | Hero + 卡片列表 + 分页 |
| 文章详情 | `/:year/:month/:day/:title/` | `en/:year/:month/:day/:title/` | 标题 + 正文 + 导航 |
| 归档 | `/archives` | `/en/archives` | 时间线列表 |
| 分类 | `/categories` | `/en/categories` | 分类卡片列表 |
| 分类详情 | `/categories/:name` | `/en/categories/:name` | 文章卡片列表 |
| 标签 | `/tags` | `/en/tags` | 标签云 + 列表 |
| 标签详情 | `/tags/:name` | `/en/tags/:name` | 文章卡片列表 |
| 关于 | `/about` | `/en/about` | 个人信息卡片 |
| 搜索 | `/search` | `/en/search` | 搜索框 + 结果列表 |

---

## 六、非功能性需求

### 6.1 性能要求

- 首页首次加载时间 ≤ 2s（静态资源）
- CSS 文件体积 ≤ 50KB（gzip 后）
- 无外部字体文件依赖（使用系统字体栈）

### 6.2 兼容性要求

- 浏览器: Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- 移动端: iOS Safari 14+, Android Chrome 90+
- 分辨率: 320px (最小) - 2560px (最大)

### 6.3 无障碍要求

- 文字对比度 ≥ 4.5:1 (WCAG AA)
- 所有交互元素可通过键盘访问
- 图片有 alt 文本
- 表单元素有 label

### 6.4 SEO 要求

- 保持现有语义化 HTML 结构
- 保持 meta 描述和 keywords
- 保持 sitemap 和 RSS 生成

---

## 七、技术实现约束

### 7.1 技术栈（不变）

| 层 | 技术 |
|----|------|
| 模板 | EJS (`.ejs` 文件) |
| 样式 | Stylus (`.styl` 文件) |
| 脚本 | Vanilla JS (jQuery 3.6.4 已引入) |
| 构建 | Hexo 生成静态文件 |

### 7.2 开发策略

| 策略 | 描述 |
|------|------|
| 渐进式改造 | 在 landscape 主题基础上逐步替换 layout 和 CSS，不从头创建主题 |
| CSS 变量优先 | 使用 CSS Custom Properties 实现深色模式，减少重复代码 |
| 保留已有定制 | 双语系统、搜索、语言切换器、空状态等已有功能全部保留 |
| 自定义文件优先 | 优先修改 `custom.styl` 和 `custom/pages.styl`，避免直接修改主题核心文件 |

### 7.3 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 破坏已有双语功能 | 高 | 修改 layout 时保留所有语言判断逻辑 |
| 深色模式遗漏页面 | 中 | 逐页面验证检查清单 |
| 移动端布局问题 | 中 | 每个页面完成后进行移动端测试 |
| 样式冲突（旧样式残留） | 低 | 使用 CSS 变量体系，逐步替换旧变量 |

---

## 八、开发优先级与阶段规划

### Phase 1: 基础重构（P0 — 必须）

| 任务 | 用户故事 | 预估工作量 |
|------|---------|-----------|
| 设计系统建立（CSS 变量、色彩、间距） | 全局 | 2h |
| Header/导航栏重构（sticky、简化、添加主题切换按钮） | US-07 | 3h |
| Hero 区域替换（渐变背景、纯文字） | US-02 | 2h |
| 首页卡片式文章列表 | US-01 | 4h |
| Footer 重构 | US-08 | 1h |
| 深色模式基础框架 | US-04 | 3h |

### Phase 2: 内容页与交互（P1 — 重要）

| 任务 | 用户故事 | 预估工作量 |
|------|---------|-----------|
| 文章详情页布局优化 | US-05 | 3h |
| 可折叠边栏实现 | US-03 | 3h |
| 归档页重构（时间线样式） | US-06 | 2h |
| 分类/标签页重构 | US-06 | 3h |
| 搜索页样式适配 | 全局 | 2h |

### Phase 3: 精细化与验证（P2 — 优化）

| 任务 | 用户故事 | 预估工作量 |
|------|---------|-----------|
| 移动端适配与测试 | 全局 | 3h |
| 深色模式全覆盖验证 | US-04 | 2h |
| 双语切换完整性检查 | 全局 | 1h |
| 过渡动画优化 | 全局 | 2h |
| 关于页样式重构 | US-05 | 1h |

---

## 九、附录

### 9.1 现有文件清单（需修改）

```
themes/landscape/
├── layout/
│   ├── layout.ejs              # 基础布局框架
│   ├── index.ejs               # 首页
│   ├── post.ejs                # 文章页
│   ├── archive.ejs             # 归档页
│   ├── tag.ejs / tags.ejs      # 标签页
│   ├── category.ejs / categories.ejs  # 分类页
│   ├── page.ejs                # 普通页面（关于页）
│   ├── search.ejs              # 搜索页
│   └── _partial/
│       ├── header.ejs          # Header 导航
│       ├── footer.ejs          # Footer
│       ├── sidebar.ejs         # 边栏
│       ├── article.ejs         # 文章卡片
│       ├── mobile-nav.ejs      # 移动端导航
│       └── language-switcher.ejs  # 语言切换器
└── source/css/
    ├── style.styl              # 主样式入口
    ├── _variables.styl         # 变量定义
    ├── _extend.styl            # 样式继承
    ├── _partial/
    │   ├── header.styl
    │   ├── footer.styl
    │   ├── article.styl
    │   ├── sidebar.styl
    │   └── ...
    └── custom/
        ├── custom.styl         # 已有自定义样式
        └── pages.styl          # 页面自定义样式
```

### 9.2 需新增的文件

```
themes/landscape/source/css/
├── _dark-mode.styl            # 深色模式变量与覆盖
├── _components/
│   ├── _card.styl             # 卡片组件样式
│   ├── _hero.styl             # Hero 区域样式
│   └── _sidebar-drawer.styl   # 可折叠边栏样式
```

---

## 十、验收总览

所有用户故事开发完成后，需满足以下总体验收条件：

- [ ] 所有 8 个用户故事的验收标准全部通过
- [ ] 中英文双语切换后，页面结构和样式一致
- [ ] 深色/浅色模式切换流畅，无遗漏页面
- [ ] 移动端 (≤ 767px) 和桌面端 (≥ 1024px) 均正常显示
- [ ] Hexo 构建无报错 (`hexo generate` 成功)
- [ ] 本地服务器预览正常 (`hexo server`)
- [ ] 搜索功能正常工作
- [ ] 边栏展开/收起交互流畅
- [ ] 现有文章页面渲染无异常
