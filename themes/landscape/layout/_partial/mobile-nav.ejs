<nav id="mobile-nav">
  <%
    var currentLang = page.lang || "zh-CN";
  %>
  <% for (var i in theme.menu){ %>
    <% if (/^[\u4e00-\u9fa5]/.test(i)) { %>
      <a href="<%- url_with_lang(theme.menu[i], currentLang) %>" class="mobile-nav-link"><%= i %></a>
    <% } %>
  <% } %>
</nav>
