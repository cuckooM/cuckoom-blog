<%
  var currentLang = page.lang || "zh-CN";
  var allPosts = site.posts.toArray();
  var filteredPosts = filter_posts_by_lang(allPosts, currentLang);
  var sortedPosts = filteredPosts.sort(function(a, b) { return b.date - a.date; });
  var limitedPosts = sortedPosts.slice(0, theme.recent_posts_limits || 5);
%>
<% if (limitedPosts.length){ %>
  <div class="widget-wrap">
    <h3 class="widget-title"><%= __('recent_posts') %></h3>
    <div class="widget">
      <ul>
        <% limitedPosts.forEach(function(post){ %>
          <li>
            <a href="<%- post_url_i18n(post) %>"><%= post.title || '(no title)' %></a>
          </li>
        <% }) %>
      </ul>
    </div>
  </div>
<% } %>
